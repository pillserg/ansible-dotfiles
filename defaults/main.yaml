---
dotfiles_scm_github_username: ""
dotfiles_aws_default_profile: ""

dotfiles_projects_dir: "{{ ansible_env.HOME }}/Projects"

dotfiles_zprezto_project_dir: "{{ dotfiles_projects_dir }}/zprezto"

dotfiles_gitconfig_user_name: "{{ dotfiles_scm_user_name }}"
dotfiles_gitconfig_user_email: "{{ dotfiles_scm_user_email }}"
dotfiles_gitconfig_github_user: "{{ dotfiles_scm_github_username }}"

dotfiles_hgrc_ui_user_name: "{{ dotfiles_scm_user_name }}"
dotfiles_hgrc_ui_user_email: "{{ dotfiles_scm_user_email }}"
